# Training data produced by M2 stage one.
#
# extraction.jsonl  (prompt, validated JSON) pairs — the fine-tune set
# attempts.jsonl    every attempt, valid or not — the compliance metric
#
# Both are append-only and contain your own transcript excerpts, so they are
# gitignored. Back them up before renting a GPU; they are the actual asset.
